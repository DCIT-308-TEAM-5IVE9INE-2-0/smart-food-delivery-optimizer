# Dataset Validation Report

This report records the result of running `scripts/generate-data/generate_dataset.py` against the current `data/*.csv` files, satisfying the "Database Team must validate IDs, missing values, duplicate records and foreign keys" action item in `docs/meetings/meeting-05.md`.

Regenerate and re-validate at any time with:

```bash
python scripts/generate-data/generate_dataset.py
```

The script both writes the CSVs and runs the checks below in one pass; it exits non-zero and lists every issue if any check fails. **Last run: all checks passed.**

## Row Counts Vs Brief Minimums

| Entity | Brief Minimum | Current Count | Status |
| --- | --- | --- | --- |
| Locations | 50 | 80 | Pass |
| Roads / Edges | 100 | 150 | Pass |
| Service Requests / Orders | 300 | 350 | Pass |
| Resources / Riders | 30 | 40 | Pass |
| Restaurants | — (not brief-mandated) | 14 | — |
| Customers | — (not brief-mandated) | 70 | — |
| Algorithm Runs | 30 | 2 | **Not yet complete** — owned by the Testing/Performance team; generated by running the console app's Performance Lab, not this script. |

## Automated Checks Performed

All checks below are implemented in `scripts/generate-data/generate_dataset.py::validate()` and run every time the script executes.

| Check | Result |
| --- | --- |
| No duplicate `location_id` values | Pass |
| No duplicate road `(from_location_id, to_location_id)` pairs | Pass |
| No road self-loops (`from_location_id == to_location_id`) | Pass |
| Every road references two existing locations | Pass |
| Location graph is fully connected (BFS reachability from an arbitrary start node reaches all 80 locations) | Pass |
| Every restaurant references an existing location | Pass |
| Every customer references an existing location | Pass |
| Every rider's home and current location reference existing locations | Pass |
| Every rider has positive capacity | Pass |
| Every order references an existing restaurant, customer, source location and destination location | Pass |
| Every order's `status` is one of `PENDING`, `ASSIGNED`, `DISPATCHED` (also enforced by a `CHECK` constraint in `database/schema.sql`) | Pass |
| `PENDING` orders have no `assigned_rider_id` | Pass |
| `ASSIGNED`/`DISPATCHED` orders have a valid `assigned_rider_id` pointing at an existing rider | Pass |
| Every order's `estimated_distance` is non-negative | Pass |
| Every order's `deadline` is strictly after its `time_submitted` | Pass |

## Dataset Shape Summary (For Report/Screenshot Evidence)

**Locations by type:** Landmark 18, Restaurant 10, Academic 10, Transport Stop 8, Hostel 8, Residential Area 6, Market 6, Service 5, Health 5, Restaurant Area 4.

**Locations by area:** University of Ghana 27, Legon Area 23, East Legon 18, Madina 6, Haatso 6.

**Roads:** 150 edges, distance range 0.03–2.67 km, average 0.39 km — consistent with a dense campus/neighbourhood network rather than long inter-city routes.

**Restaurants:** 14, of which 12 `OPEN` and 2 `CLOSED` (status variety, not a functional gate in the current console app).

**Riders:** 40, of which 15 `AVAILABLE` and 25 `BUSY` at seed time (matching the orders already `ASSIGNED`/`DISPATCHED`); vehicle mix Motorbike 18, Bicycle 15, Tricycle 7.

**Orders:** 350, status split PENDING 245 / ASSIGNED 70 / DISPATCHED 35 (roughly the 70/20/10 split agreed for a realistic operational snapshot, oldest orders furthest along); urgency distribution skewed toward the middle of the 1–5 range (urgency 2: 116, 3: 94, 1: 63, 4: 50, 5: 27); estimated delivery distance range 0.09–7.37 km, average 3.28 km.

## Known Gaps / Follow-Up

- `data/algorithm_runs.csv` still only has 2 seed rows against the brief's 30-row minimum — needs the Testing/Performance team to run the console app's Performance Lab at report-scale input sizes (see `docs/performance-plan.md` and the outstanding item in `docs/meetings/meeting-05.md`, section 3.5).
- This validation confirms structural/referential correctness (FKs, duplicates, connectivity, status/rider consistency). It does not substitute for the Database Team manually spot-checking a sample of rows against real-world plausibility, which should still be captured as report screenshots per the Meeting 5 action register.
